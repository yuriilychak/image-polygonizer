# Image Polygonizer Algorithm (WASM)

This package contains the core algorithms for image polygonization, compiled to WebAssembly.

## Building

```bash
npm run build
```

This will compile the Rust code to WebAssembly and output to the `dist` folder.

## Development

```bash
npm run dev
```

Build in development mode with debug symbols.
